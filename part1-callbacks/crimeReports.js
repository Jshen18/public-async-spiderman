const fs = require('fs');
const path = require('path');

const crimeReportsDir = './data/reports';

// step 5 - another sequence of async that involves iteration
function getAll(callback) {
  // TODO - your code here!
}

// step 6 - use that function elsewhere, with a filter
function find(options, callback) {
  // TODO - your code here!
}

module.exports = {
  getAll,
  find
}
